#!groovy

@Library("Infrastructure@2.7.1")

def type = "java"
def product = "opal"
def component = "logging-service-integration-lib"

withPipeline(type, product, component) {
    nonDeployableApp()
    releaseOnMerge()
}