#!groovy

@Library("Infrastructure@2.10.0")

def type = "java"
def product = "opal"
def component = "logging-service-integration-lib"

withPipeline(type, product, component) {
    nonDeployableApp()
    releaseOnMerge()
}